{
  "name": "safestart",
  "version": "2.1.0",
  "private": true,
  "description": "SafeStart — parental controls made simple. Curated step-by-step guides, prerendered at real URLs, with live research for anything not curated yet.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && node scripts/dev-server.js",
    "check": "node scripts/check.js",
    "test": "node scripts/build.js && node scripts/check.js && node scripts/api-test.js && node scripts/browser-test.js"
  }
}
